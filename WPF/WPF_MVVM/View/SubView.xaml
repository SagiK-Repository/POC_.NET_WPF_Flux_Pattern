<Window x:Class="WPF_MVVM.View.SubView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WPF_MVVM.ViewModel"
        mc:Ignorable="d"
        Title="SubWindowView"
        Height="450"
        Width="800"
        Name="SubViewWindow"
        d:DataContext="{d:DesignInstance {x:Type vm:SubViewModel}}">
    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <TextBlock Text="숫자를 입력하세요:" />
        <TextBox x:Name="numberTextBox"
                 Text="{Binding EnterString, Mode=TwoWay}"
                 Width="100"
                 Margin="0 10" />
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="저장"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding ElementName=SubViewWindow}"
                    Margin="5" />
            <Button Content="취소"
                    Command="{Binding CancelCpmmand}"
                    CommandParameter="{Binding ElementName=SubViewWindow}"
                    Margin="5" />
        </StackPanel>
    </StackPanel>
</Window>